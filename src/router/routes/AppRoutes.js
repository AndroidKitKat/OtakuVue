// Import the views for the routes.
import Home from '@/views/Home'

// Define the routes.
// exclude: when on this route remove the main navigation.
export default [{ path: '/', name: 'home', component: Home, title: 'Home' }]
